# dependencies
node_modules/
# build artifacts
dist/
*.exe
# OS / editor files
.vscode/
.DS_Store
Thumbs.db
# Git metadata (if you ever nested repos)
.git/

# runtime data (RFQ store, audit log) — contains customer PII, never commit
parts-store/.data/
.data/
# next build output
parts-store/.next/
*.tsbuildinfo

# Operational backups of .data/ — contain customer PII and quote pricing.
# Store these encrypted and off-box; never commit one.
.backups/
